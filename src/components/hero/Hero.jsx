import React from "react";
import { HeroWrapper } from "./hero.elements";
import { Card } from "../card/Card";

export const Hero = () => {
  return (
    <HeroWrapper id="home">
      <Card />
    </HeroWrapper>
  );
};
